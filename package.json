{
  "name": "plantilla-pokedex",
  "version": "1.0.0",
  "description": "this is a project for the templates of the pokedex app",
  "main": "index.js",
  "scripts": {
    "start": "npx tailwindcss -i ./src/styles/input.css -o ./dist/output.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "pokedex",
    "pokemon"
  ],
  "author": "evalenzuela",
  "license": "ISC",
  "devDependencies": {
    "postcss": "^8.4.5",
    "tailwindcss": "^3.0.12"
  },
  "dependencies": {
    "live-server": "^1.2.1"
  }
}
